eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  boot:
    admin:
      client:
        url: http://localhost:9090

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env

  endpoint:
    health:
      show-details: ALWAYS

  tracing:
    sampling:
      probability: 1.0   # 100% tracing (prod → 0.1)

  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans

jwt:
  secret: STvtWyNSbhkDvUZfM3LU8przbgeSLzIrjrKhSby4xk0=
  access-token-expiration: 900000
  refresh-token-expiration: 604800000
